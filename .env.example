# === Core Settings ===
APP_ENV=dev
APP_NAME=AI Hospitality Agent
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO

# === Google ADK / Gemini (stubbed) ===
GOOGLE_ADK_API_KEY=replace_with_key_if_using_real_adk

# === GCP / BigQuery (optional) ===
GCP_PROJECT_ID=your-project-id
BIGQUERY_DATASET=hotel_ops
BIGQUERY_TABLE_ANALYTICS=analytics_events
# If not provided/authenticated, the app will use a local mock store.

# === Security ===
CORS_ORIGINS=*
